
new Picker(document.querySelector('.picker-from'), {
    controls: true,
    format: 'YYYY-MM-DD',
    headers: true
});

new Picker(document.querySelector('.picker-to'), {
    controls: true,
    format: 'YYYY-MM-DD',
    headers: true
});